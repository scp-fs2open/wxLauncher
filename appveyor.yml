environment:
        matrix:
                - WXVER: "2.8"
platform:
        - x86
configuration:
        - RelDbgInfo
        - Debug
before_build:
        - ps1: "%APPVEYOR_BUILD_FOLDER%/ci/appveyor/load_wxwidgets.ps1"
build_script:
        - cmd: "%APPVEYOR_BUILD_FOLDER%/ci/appveyor/build.cmd"
